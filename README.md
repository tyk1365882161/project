# 🛒 E-Commerce Website (Java Spring Boot)

> 华南理工大学《网络应用开发》课程期末项目

这是一个基于 **Spring Boot 3.x** 和 **Thymeleaf** 开发的简易电子商务网站系统。项目实现了从用户浏览、下单到结算的完整购物流程，并包含一个功能完善的后台管理系统，用于商品管理和销售数据统计。

---

## 👨‍💻 作者信息 (Author)

* **姓名**：谭彦铿
* **学号**：202330451611
* **班级**：23级计科2班
* **提交日期**：2025-12-16

---

## 🔗 在线演示 (Live Demo)

* [cite_start]**访问地址**：[http://8.138.56.217:8080](http://8.138.56.217:8080) [cite: 8]
* **测试账号**：
    * [cite_start]**普通用户**：`user` / `123456`  (用于购物流程测试) [cite: 9]
    * [cite_start]**管理员**：`admin` / `123456` (用于后台管理测试) [cite: 9]

---

## 🛠️ 技术栈 (Tech Stack)

本项目采用前后端分离（逻辑上）但在同一工程中部署的架构（Monolithic with SSR）。

* [cite_start]**开发语言**：Java 17 
* [cite_start]**核心框架**：Spring Boot 3.x + Spring Data JPA 
* [cite_start]**前端技术**：Thymeleaf 模板引擎 + Bootstrap 
* [cite_start]**数据库**：MySQL 8.0 
* [cite_start]**工具链**：Maven, Git, VScode/IDEA 

---

## 📂 项目结构说明 (Project Structure)

本项目基于 Maven 标准目录结构，采用经典的分层架构设计：

```text
src/main/java/com/example/mall
├── common          // 🛠️ 通用模块
│   └── Result.java       // 统一响应结果封装类
│   └── GlobalExceptionHandler.java // 全局异常处理
├── config          // ⚙️ 配置模块
│   └── WebMvcConfig.java // Web配置 (如拦截器注册)
├── controller      // 🎮 控制层 (处理 HTTP 请求)
│   ├── AuthController.java    // 用户注册与登录接口
│   ├── ProductController.java // 商品列表展示与搜索接口
│   └── OrderController.java   // 购物车与订单处理接口
├── entity          // 🗄️ 实体层 (JPA Entity, 对应数据库表)
│   ├── User.java        // 用户实体 (对应 t_user)
│   ├── Product.java     // 商品实体 (对应 t_product)
│   └── Order.java       // 订单实体 (对应 t_order)
├── repository      // 💾 持久层 (Spring Data JPA 接口)
├── service         // 🧠 业务逻辑层
│   └── impl             // 业务接口实现类 (核心逻辑如库存扣减)
└── utils           // 🧰 工具类
    └── Md5Util.java     // 密码加密工具
